{$DOMAIN:localhost} {
    # Reverse proxy to Flask app
    reverse_proxy app:8080
    
    # Enable automatic HTTPS with Let's Encrypt (HTTP challenge - works with any DNS provider)
    tls {
        # HTTP challenge - works with Namecheap and other DNS providers
        # No API token needed
    }
    
    # Logging
    log {
        output file /var/log/caddy/access.log
        format json
    }
    
    # Security headers
    header {
        X-Frame-Options "SAMEORIGIN"
        X-Content-Type-Options "nosniff"
        X-XSS-Protection "1; mode=block"
        Referrer-Policy "strict-origin-when-cross-origin"
    }
}
